{
  "enabled": true,
  "device": {
    "host": "clawd-mochi.local",
    "timeout_ms": 500
  },
  "log_file": "~/.cache/clawd-mochi.log",
  "statusline": { "enabled": true },
  "_notes": "events overrides the built-in hook -> Event table hook by hook; null mutes a hook. Schema and the default table: skills/setup/reference.md in the plugin.",
  "events": {}
}
